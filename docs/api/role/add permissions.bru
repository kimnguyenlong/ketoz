meta {
  name: add permissions
  type: http
  seq: 4
}

post {
  url: {{api_host}}/roles/:id/permissions
  body: json
  auth: inherit
}

params:path {
  id: dev
}

body:json {
  {
    "permissions": [
      {
        "resource_id": "projects",
        "action": "view"
      }
    ]
  }
}
